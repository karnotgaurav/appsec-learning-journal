java basics need to know for appsec

1. variables : its the simple variables like 
        const pi = 3.14 in this the value dosen't change
        let score = 31 in this u can change 

2. function : function (add(a,b)){
    return a+b;
            }
const result =add(2,3);

3. arrow funtion it is for the one line make it simple 
    const double = n => n * 2;
    const double = n =>n^2;


